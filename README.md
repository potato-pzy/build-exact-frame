
  # Build Exact Frame

  This is a code bundle for Build Exact Frame. The original project is available at https://www.figma.com/design/FSkzRhXfogAvHgo4WueanL/Build-Exact-Frame.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  